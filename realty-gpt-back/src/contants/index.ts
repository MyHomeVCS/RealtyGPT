import { ESupportedLanguages } from 'src/interfaces';

export const SUPPORTED_LANGUAGES: Record<ESupportedLanguages, ESupportedLanguages> = {
  armenian: ESupportedLanguages.armenian,
  russian: ESupportedLanguages.russian,
  english: ESupportedLanguages.english,
};
